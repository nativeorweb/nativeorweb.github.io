<?xml version="1.0" encoding="utf-8"?>
    <layer-list xmlns:android="http://schemas.android.com/apk/res/android">

        <!-- Seekbar from right to left -> progress and background exchanged -->
        
        <item 
            android:id="@android:id/background" 
            android:drawable="@drawable/seekbar_right_progress" />

        <item android:id="@android:id/progress">
           <clip android:drawable="@drawable/seekbar_right_background" />
        </item>
  </layer-list>